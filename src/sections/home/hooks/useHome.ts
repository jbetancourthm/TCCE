/**
 * Hooks del módulo Home — reexportaciones centralizadas.
 *
 * | Hook | Rol |
 * |------|-----|
 * | `useHomeScrollIndicator` | Visibilidad del indicador de scroll en la home (ocultar al scroll, reaparecer tras idle). |
 */

export { useHomeScrollIndicator } from './useHomeScrollIndicator'
