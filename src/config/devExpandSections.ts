/**
 * Si es true, se muestran todas las secciones dinámicas a la vez (sin depender de tarjetas/pestañas).
 * Pon en true al volver a desarrollar con todo visible.
 */
export const EXPAND_ALL_SECTIONS = false
