# Copia a .env.local (no commitear). Solo variables públicas: el prefijo VITE_ expone el valor en el bundle del navegador.
# Nunca pongas claves API, contraseñas ni tokens en variables VITE_*.
VITE_CONTACT_API_URL=http://localhost:3001
